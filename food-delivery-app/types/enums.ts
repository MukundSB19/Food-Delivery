export enum OrderStatus {
  Pending = "pending",
  Preparing = "preparing",
  Delivering = "delivering",
  Delivered = "delivered",
  Cancelled = "cancelled",
}

export enum PaymentMethodType {
  Card = "card",
  UPI = "upi",
  COD = "cod",
}
