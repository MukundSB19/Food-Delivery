import { View, Text } from "react-native";
import { AppScreenWrapper, DrawerComponent } from "@/components";

const faq = () => {
    return (
        <AppScreenWrapper>
            <View>
                <Text>faq</Text>
                <DrawerComponent />
            </View>
        </AppScreenWrapper>
    );
};

export default faq;
